<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.heeexy.example.dao.WxFansDao">
    <!--添加粉丝关注用户-->
    <insert id="addByFans" parameterType="com.alibaba.fastjson.JSONObject">
        insert  into  fly_fans (onuser_id, user_id, state) values (#{onUserId},#{userId},1);
    </insert>
    <!--计算关注用户数-->
    <select id="countByUserId" parameterType="com.alibaba.fastjson.JSONObject" resultType="com.alibaba.fastjson.JSONObject">
          select  count(onuser_id) fansNumber from fly_fans where state =1 and onuser_id =#{onUserId};
    </select>
    <!--查找关注用户列表(小程序显示列表)-->
    <select id="getListByUserId" parameterType="com.alibaba.fastjson.JSONObject" resultType="com.alibaba.fastjson.JSONObject">
      select
          fans.fans_id fansId,fans.onuser_id onUserId,users.user_name  onUserName ,
          u.user_id userId,u.user_name  userName,fans.state  state,
          DATE_FORMAT(fans.create_time, '%Y.%m.%d %T') createTime,
          DATE_FORMAT(fans.update_time, '%Y.%m.%d %T') updateTime
      from  fly_fans fans,fly_user users,fly_user u
      where users.user_id =fans.onuser_id and  u.user_id =fans.user_id and
            fans.onuser_id =#{onUserId} and fans.state = 1
    </select>

    <!--查找用户是否有关注信息-->
    <select id="getUserFans" parameterType="com.alibaba.fastjson.JSONObject" resultType="com.alibaba.fastjson.JSONObject">
        select  onuser_id onUserId,user_id userId,state state from fly_fans
        where onuser_id =#{onUserId} and user_id =#{userId};
    </select>
    <!--修改关注用户状态取消-->
    <update id="updateFansByUserId" parameterType="com.alibaba.fastjson.JSONObject">
  /*  update fly_fans set state = 0  where fans_id =#{fansId}*/
  update fly_fans set state = 0  where onuser_id =#{onUserId} and user_id =#{userId}
    </update>
    <!--添加关注用户状态-->
    <update id="updateFansStateByUserId" parameterType="com.alibaba.fastjson.JSONObject">
    /*update fly_fans set state = 1  where fans_id =#{fansId}*/
    update fly_fans set state = 1  where onuser_id =#{onUserId} and user_id =#{userId}
    </update>
    <!--查找所有用户粉丝关注表（后台显示）-->
    <select id="getListFans" parameterType="com.alibaba.fastjson.JSONObject" resultType="com.alibaba.fastjson.JSONObject">
       select
            fans.fans_id fansId,fans.onuser_id onUserId,users.user_name  onUserName ,
            u.user_id userId,u.user_name  userName,fans.state  state,
            DATE_FORMAT(fans.create_time, '%Y.%m.%d %T') createTime,
            DATE_FORMAT(fans.update_time, '%Y.%m.%d %T') updateTime
       from  fly_fans fans,fly_user users,fly_user u
        <where>
            users.user_id =fans.onuser_id and  u.user_id =fans.user_id and fans.state =1
              <if test="userName != null and userName != ''">
                and  users.user_name LIKE CONCAT(CONCAT('%',#{userName}),'%')
              </if>
        </where>

          LIMIT #{offSet}, #{pageRow};
    </select>
    <!--计算用户数(后台)-->
    <select id="countUserFans" parameterType="com.alibaba.fastjson.JSONObject" resultType="java.lang.Integer">
        select count(0) from fly_fans
    </select>
    <!--查找用户未关注的-->
    <select id="getListMoreFansById" parameterType="com.alibaba.fastjson.JSONObject" resultType="com.alibaba.fastjson.JSONObject">
        select  fans.fans_id fansId,fans.user_id userId,u.user_name userName
        from  fly_fans fans,fly_user u
        where u.user_id=fans.user_id and fans.onuser_id =#{onUserId} and fans.state =0
    </select>
    <!--批量关注-->
    <update id="addMoreFans" parameterType="java.util.Map">
        update fly_fans set fly_fans.state =1 where fans_id =#{fansId}
     </update>
    <!--我的关注-->
    <select id="myFans" resultType="com.alibaba.fastjson.JSONObject" parameterType="com.alibaba.fastjson.JSONObject">
        select f.fans_id fansId,u.user_id fansUserId,u.user_name fansName,u.user_photo fansImg
        from fly_fans f,fly_user u
        where u.user_id= f.user_id and f.onuser_id =#{uid} and f.state =1
    </select>
</mapper>